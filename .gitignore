
# Local capture recordings — large test artifacts, not source.
# Local transcription venv (faster-whisper/mlx-audio) — recreate with uv, see analyze/README.
# Per-user auto-pack config (watch dirs + packs dir) — written by setup, machine-specific.
*.pyc
.DS_Store
.hermes/
.playwright-mcp/
.venv/
__pycache__/
analyze/autopack.config.json
outputs/
